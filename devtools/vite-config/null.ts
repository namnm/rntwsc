/* eslint-disable import/no-default-export */

// Stub for native-only modules react-native-web has no equivalent for, e.g.
// codegenNativeComponent, which some native spec files call unconditionally
// at module top level - must stay callable, see index.ts's resolveAlias.
export default () => null
